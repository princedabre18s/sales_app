streamlit>=1.42.0       # For Streamlit (minimum version specified)
pandas>=2.2.0           # For DataFrame operations (minimum version specified)
numpy>=2.0.0            # For numerical operations (minimum version specified)
psycopg2-binary>=2.9.9  # PostgreSQL adapter (minimum version specified)
sqlalchemy>=2.0.0       # SQL toolkit and ORM (minimum version specified)
matplotlib>=3.9.0       # For basic plotting (minimum version specified)
seaborn>=0.13.0         # Statistical data visualization (minimum version specified)
plotly>=5.15.0          # For interactive charts (minimum version specified)
openpyxl>=3.1.0         # For reading/writing Excel files (minimum version specified)
python-dotenv>=1.0.1    # For loading environment variables from .env
ollama>=0.1.7           # For interacting with the Ollama API (adjust based on your installation)
scipy>=1.13.0           # For statistical functions (e.g., stats module)
flask>=3.0.3            # For the Flask server in app.py
flask-cors>=4.0.0       # For enabling CORS in Flask
cachetools>=5.3.3       # For LRU caching (used with lru_cache)
